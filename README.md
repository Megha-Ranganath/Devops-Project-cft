# Devops-Project-cft
git init 
git remote add origin < git url>
git status
git checkout -b stagging
vi text.txt
git add .
git commit -m 'Added text file'
git pull origin stagging --rebase
git push origin stagging
